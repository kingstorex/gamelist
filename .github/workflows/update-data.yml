name: Auto-Update Game Data

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: 

permissions:
  contents: write 

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch API, filter games, strip details, and save to data.json
        run: |
          # Fetch JSON, filter out unwanted games, then extract only necessary fields
          curl -sL https://steamunlockonennabe.duckdns.org/api/onennabe | jq 'map(select(.requires_membership == false and .hypervisor_bypass == 0) | {name, appid, downloads})' > data.json

      - name: Commit and push if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data.json
          # This line checks if data.json actually changed before trying to commit
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update and filter game data" && git push)
